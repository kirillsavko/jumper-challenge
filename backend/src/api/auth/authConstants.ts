/** Message that should be signed by the user for logging in */
export const MESSAGE_TO_SIGN = 'Hello, sign this message for authentication!';
/** Name of the cookie field where the jwt token is stored at */
export const TOKEN_COOKIE_NAME = 'jwt-token';
export const ONE_HOUR_IN_MS = 60 * 60 * 1000;
